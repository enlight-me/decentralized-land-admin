import React from 'react';
import { fade, makeStyles } from '@material-ui/core/styles';

export default function ParcelMapPopup(props) {

  return (
    // <Popup><p>Informations</p><pre>CSC: ${props.properties.csc}</pre></Popup>
    <div></div>
  );
}
